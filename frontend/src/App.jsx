/**
 * AI 身体状态计划管家 — v2 Web App
 *
 * v2 skeleton starting here.
 * 当前方向见 `docs/plan.md`。开工前必读清单见 memory `project_progress.md`。
 *
 * 开发顺序（见 plan.md）：
 *   第 0 步：视觉 HTML 原型（prototype.html）+ 多用户改造
 *   第 1 步：三页 Tab 骨架 + 专注遮罩
 *   第 2 步：聊天室接 FastAPI
 *   第 3 步：白鼬桌宠 11 状态
 *   第 4 步：数据统计页
 *   第 5 步：打磨 + 部署
 *
 * 历史的像素空间实现见 git tag `pixel-v0.3`（已归档，不复活）。
 */

export default function App() {
  return (
    <div style={{ padding: '24px', fontFamily: 'system-ui, sans-serif' }}>
      <h1>AI 身体状态计划管家 v2</h1>
      <p>Skeleton. 按 docs/plan.md 开发顺序开工。</p>
    </div>
  )
}
