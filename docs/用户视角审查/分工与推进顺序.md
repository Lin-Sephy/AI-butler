# 分工与推进顺序

> 2026-04-22 写 · 配合 A/B/C/D 四视角文档使用
>
> **谁在读这份**：Sephy 开多 agent 并行推进时让每个 agent 先读这份，再读对应视角文档。这是**协调文件**，不是新问题清单。

---

## 已完成的条目（2026-04-22 起更新）

开始工作前先扫一下这段，避免重复改或撞车。

- ✅ **A-1**：FocusOverlay 右下角 🦦 → ❄（`FocusOverlay.jsx:147`）
- ✅ **A-2**：FocusOverlay"🎧 白噪音（待接入）"占位删除
- ✅ **A-5**：冷启动 App.jsx 渐进式文案 3 段（`App.jsx:20-36`，5s/15s 切换）
- ✅ **B-1**：FocusOverlay 切换 `Date.now()` 基准（`FocusOverlay.jsx:8-52`，含 `pausedAccumRef`+`pauseStartRef` 两 ref 管暂停时长 + visibilitychange 立即补齐，比方案更细致）

**意义**：B-1 毁数据的根因已拔掉，所有新开 agent 可以放心基于现有 FocusOverlay 改动，不用等前置修复。

---

## 并行陷阱回顾（必读）

1. **基础设施级的改动（C 视角设计系统）必须单线**，不要和别的 agent 并行。C 是所有其他改动的前提——如果别 agent 在抽 Button/Modal，这边又在做新功能，要么等、要么冲突。**C 最后做**。
2. **多 agent 分工按文件，不按功能**。避免多个 agent 改 `TasksPage.jsx` 或 `App.jsx`。看下面"按文件归属分层"。
3. **💎 反杀系列**（D 视角第二层：数据页叙事 / 小白探头 / 松针林 / AI 主动关怀）**不要让 agent 自由发挥**——需要先和 Sephy 对产品方向。
4. 每个 agent 完成后**在自己的分支上 commit + 先不 push**，等 Sephy 调度合并顺序。

---

## 按文件归属分层（推荐的 Agent 分工）

### Agent 1 — 后端 core / db 链路

**文件归属**：`core/` + `db/` + `api.py` + `prompts/` + `tests/`

**任务**：
- 🔥 **B-3** idle 任务 2 天自动结转 + abandoned 只保留 2 天（`core/task_manager.py:get_today_tasks` / `db/database.py:get_tasks_recent` / `core/plan_tools.py:_tool_query_tasks`）—— 2026-04-22 Sephy 定稿方案，见 B 文档
- 🔥 **B-4** create_tasks 工具加 scheduled_at 字段（`core/plan_tools.py:124-163` schema + `_tool_create_tasks` 实现分支走 `create_scheduled_task`）
- 💡 **B-5** 印象传给 prompt 的数量上限 10-15 条（`core/memory.py:get_confirmed_impressions_text`）
- 💡 **B-6** `load_session_messages` 加 limit 参数（`db/database.py:285`）+ ChatContext 首次加载只取最近 50 条

**不动**：任何前端文件。

**参考**：B 文档全文。

---

### Agent 2 — 前端 pages / components 的 bug 修复

**文件归属**：`frontend/src/pages/*.jsx` + `frontend/src/components/*.jsx`（但**排除** Agent 3 / Agent 4 专管的那几个）

**任务**：
- 💡 **A-3** FocusOverlay 完成按钮短时间保护（开始 30s 内点完成弹确认）（`FocusOverlay.jsx`）
- 💡 **A-4** 砍掉 NewTaskModal 的"专注/休息"选项（`NewTaskModal.jsx` + 需要同步看 PlanConfirmModal 有没有依赖）
- 🔥 **A-0（Sephy 核心痛点）** 闲聊模式前端关键词触发"切任务模式"胶囊按钮（`ChatPage.jsx`）—— 扫用户消息里 "安排 / 计划 / 改 / 删 / 取消 / 推到 / 加一个 / 今天做 / 任务" 等词，匹配到就在输入框上方浮一个按钮
- 💡 **A-0** TasksPage handleAction 省一次 fetch（`TasksPage.jsx:41-43`）
- 💡 **A-0** thinking 气泡动态文案（3s / 8s 两段切换）（`ChatPage.jsx` stoatState 逻辑）

**不动**：FocusOverlay 的番茄循环逻辑（Agent 3 管）；SettingsPanel / LlmConfig（动代码风险高，先不动）；设计系统抽取（Agent 4 管）。

**参考**：A 文档 + B 文档对"页面刷新慢"的分析段。

---

### Agent 3 — 番茄闭环新功能

**文件归属**：主要动 `FocusOverlay.jsx` + 新增 assets（音源）+ 可能扩展 `App.jsx` 的 stats tab

**任务**：
- 🔥 **D-1** FocusOverlay 增加 `phase: 'work'|'rest'|'asking'` 状态，番茄自动循环（5 分钟短休 / 4 个番茄后 15 分钟长休）
- 🔥 **D-2** 白噪音：雨 / 篝火 / 林间三个音源 + 音量控件 + 静音开关 + localStorage 记上次选择
- 🔥 **D-4** 专注结束提示：短音效 + Web Notification（请求授权）+ 视觉微动画
- 💡 **D-5** TasksPage 顶部"🔥 快速开始 25 分钟"胶囊按钮

**不动**：Agent 2 同时在动的 TasksPage / ChatPage 里不属于这几条的部分；D-3 数据页（那条需要 Sephy 先对产品方向 → 见下文）。

**参考**：D 文档第一层全文。

**注意**：Agent 3 动 FocusOverlay 的改动和 Agent 2 的 A-3 可能冲突（都改同一个文件）。**建议顺序**：Agent 2 先完成 A-3 交付 → 再让 Agent 3 开始做 D-1/D-4。或者更简单：把 A-3（完成按钮保护）交给 Agent 3 顺手做，和 D-1 一起。

---

### Agent 4 — 设计系统抽取（最后做）

**等所有其他 agent 的改动都 merge 完成再开**。因为抽设计系统时要一次性扫所有组件替换样式。

**文件归属**：新增 `frontend/src/components/ui/` 目录 + 改所有现有组件

**任务（建议顺序）**：
1. 抽 `<Button>` / `<IconBtn>` 到 `ui/Button.jsx`（C-3）
2. 抽 `<Modal>` 到 `ui/Modal.jsx`，内置 focus trap + Escape + aria-*（C-7）
3. 抽 `<ErrorBanner>` / `<ConfirmDialog>` 到 `ui/Feedback.jsx`（C-1 / C-2）
4. 写 `frontend/src/design-tokens.css` 扩展字号 / 圆角 / 动画时长档位（C-9）
5. 全项目迁移（一边迁一边清 C-3 到 C-6）
6. 加全局 `button:focus-visible` 样式（C-7 起步）
7. 图标按钮的 `title` 改 `aria-label`（C-7 扫尾）

**参考**：C 文档全文。

---

## Sephy 需要先对方案、不要让 agent 自己做的事

这些 agent 拿到需求会自由发挥，容易偏——**必须 Sephy 先定方向**：

- 💎 **D-3 数据页**：不做柱状图做小白叙事文案（见 D 视角"反杀 1"）。要定：用 DS 实时生成还是模板拼？触发频率？
- 💎 **D 视角反杀 2**：番茄结束是小白从角落探头（配桌宠第 3 步 SVG）。要定：动画时长、白鼬动作素材、气泡文案模板
- 💎 **D 视角反杀 3**：连续打卡 = 松针林茂密度。要定：几天对应几棵松针？上线到哪个页面？
- 💎 **D 视角反杀 4**：AI 主动关怀触发。要定：什么 session 状态触发？prompt 怎么写？

---

## 推进顺序总图

```
[已完成] A-1 / A-2 / A-5 / B-1
         ↓
[现在并行] Agent 1（后端链路）│ Agent 2（前端 bug）│ Agent 3（番茄闭环）
         ↓                      ↓                    ↓
         都合并回主分支
         ↓
[独自推进] Agent 4（设计系统抽取 + 迁移全部组件）
         ↓
[对完方案再做] 💎 D-3 数据页 + 反杀系列（桌宠第 3 步做完之后）
```

---

## 最后一条给 Agent 们的提醒

**不要自己解决依赖**。如果你在推进某条时发现它依赖另一个 agent 的改动，**停下来告诉 Sephy**。不要自己 diff 别的 agent 的代码硬接。并行分工的关键是**边界清晰**，自己越界反而让合并更乱。

按 feedback "卡住就停别瞎撞"的规矩办。
