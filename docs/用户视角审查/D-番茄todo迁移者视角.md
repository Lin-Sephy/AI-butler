# D 视角：番茄 todo 忠实用户能不能迁过来再不回去

> 2026-04-22 小克 review
>
> 视角定义：一个把番茄 todo 当每日工具用了好几年的老用户。她打开我们的产品是来**试用**的——几天之内能让她"这个更好用，我不回去了"就留下，否则回流。
>
> 这一视角的核心判断：**差异化要够香 + 基础能力不能比竞品差**。如果差异化不够香，用户不会来；如果基础能力比番茄 todo 差，用户来了也会跑。

---

## 先理解这类用户的心理锚点

番茄 todo 老用户对下列体验有"**成瘾式**"依赖。这些不是"功能"，是她每天的操作记忆：

1. **番茄自动循环**：25 专注 → 5 休息 → 25 专注 → 15 长休。不需要思考，跟着节奏走。
2. **白噪音陪伴**：打开 app = 打开雨声 / 咖啡馆 / 图书馆 / 自然声，是**专注状态的锚**。
3. **数据可视化**：每天看今天几个番茄，每周看趋势，年底看"今年专注了 XXX 小时"。数据是**成就感回路**。
4. **连续打卡 / 累计番茄**：连续 200 天不断卡的成就感——一断了就塌方。
5. **快速开始**："就先专注 25 分钟什么都行"的入口。不用建任务、不用想做什么。
6. **专注结束提示**：番茄归零的"叮"一声 / 推送通知，是**一次专注闭环的仪式**。

**这六条里当前我们一条都没完整做到**。下面按"先看基础能力守得住不，再看差异化能不能反杀"两层来审。

---

## 第一层：基础能力守得住吗（番茄 todo 用户不会为之惊艳，但缺了就会回流）

### 🔥 D-1. 没有"番茄 → 休息"自动循环

**代码现状**：`FocusOverlay.jsx:19-24` 倒计时归零就 `onComplete(task.id)`，任务状态变 completed，遮罩 unmount，用户回到任务列表。

**番茄 todo 用户预期**：专注 25 分钟结束后自动切 5 分钟休息倒计时 + 小白提示"休息 5 分钟哦"；休息结束后询问"再来一个番茄吗？"。整个流程用户不用操作。

**当前有多别扭**：
- 一个番茄完成 → 回到任务列表 → 手动建"休息 5 分钟"任务 → task_type 选 rest → 开始 → 休息完 → 再建下一个工作任务
- 把一个**流畅的心流打成 5 段操作**
- 对番茄 todo 用户是直接劝退

**小克倾向**：FocusOverlay 增加"自动续接"模式。work 任务完成后不直接 unmount，而是切换成 rest 倒计时（默认 5 分钟，长休每 4 个番茄 15 分钟）；rest 结束后问"再来一轮吗？"（默认按下一个 idle 任务，或用"继续专注相同事情"）。

实现路径：FocusOverlay 维持一个 `phase: 'work' | 'rest' | 'asking'` 的内部状态，不 unmount；完成时调 API 后切 phase。

**工作量**：3-4h（含番茄计数 / 长休规则）。

**优先级**：**最高**。这是"迁过来再回不去"的最大单点瓶颈。

---

### 🔥 D-2. 没有白噪音

**代码现状**：`FocusOverlay.jsx:126` 写"🎧 白噪音（待接入）"占位。C 视角已经提到"待接入"是 bug——D 视角加码：**白噪音对番茄 todo 用户不是 nice-to-have，是核心功能**。

**番茄 todo 用户预期**：开始专注时默认播放上次用的白噪音；可切换雨 / 咖啡馆 / 图书馆 / 篝火 / 海浪。

**技术实现并不难**：HTML5 `<audio loop>` + 3-5 个公共音源（Free Sound / Zapsplat 有免费可商用）。web 端不涉及权限。大概 1-2h 能做出最简版本。

**小克倾向**：
- 先上 3 个：**雨声 / 篝火 / 林间** —— 选这三个能呼应"松针雪地小白鼬"调性
- 音量控件 + 静音开关 + 上次选择落 localStorage
- 建议音源不用 AI 生成，找真实录音，调性差距很大

**优先级**：**最高**。是每日体感的锚。

---

### 🔥 D-3. 没有数据统计页面

**代码现状**：`App.jsx:47` `{tab === 'stats' && <Placeholder title="数据" />}` —— 数据 tab 是空 Placeholder。

**后端其实已经有了**：`core/plan_tools.py:199` `_tool_query_stats` 返回完成数、放弃数、完成率、平均专注时长、最常专注时段、最常做的事。`api.py:725` 还单独开了 `/api/stats` endpoint 复用它。

**缺的只是前端**。

**番茄 todo 用户预期**：至少三块——
- 今日：几个番茄 / 多少分钟 / 完成几个任务
- 本周：7 天折线或柱状图
- 累计：总番茄数 / 总时长 / 连续打卡天数

**小克倾向**：不用立刻做得像番茄 todo 那么花哨。**最简版**是三张小卡片：
1. 今日番茄数 + 今日总专注分钟
2. 本周 7 天柱状图（用 SVG 手画，不引图表库）
3. 连续打卡天数（从 action_log 或 task completed_at 查就行）

**重要：不要直接抄柱状图**。下面第二层会说怎么用产品调性做。

**优先级**：**最高**。空的数据 tab 对试用者等于"这 app 不严肃"。

---

### 🔥 D-4. 没有"专注结束提示"

**代码现状**：`FocusOverlay.jsx:21` 倒计时归零只是 `onComplete(task.id)`，没声音、没通知、没视觉提醒。

**番茄 todo 用户预期**：番茄归零 → 声音提示（钟声 / 柔和叮 / 自定义）+ 浏览器通知（如果用户授权）+ 视觉反馈（闪烁 / 过渡动画）。

**特别重要的场景**：用户专注中切到别的 tab（比如看参考资料），番茄结束她根本不知道。没有提示 = 番茄白跑。

**小克倾向**：
- 结束时播一个短音效（1-2 秒钟声或小白"喵"一声——调性上走后者）
- 请求 `Notification.permission`，授权后用 Web Notification API 推"小白：25 分钟到啦"
- 视觉上：计时器 `00:00` 后维持屏幕 1-2 秒，给小白一个"小跳"或"冒爱心"的微动画，再切下一阶段

**工作量**：2-3h。

**优先级**：**高**。配合 D-1 才算一个完整番茄闭环。

---

### 💡 D-5. 没有"快速开始"入口

**代码现状**：`TasksPage.jsx` 必须先 `新建任务` → 填名字 → 选时长 → 点"开始"。

**番茄 todo 用户场景**：心情烦躁 / 没想好做什么，但"就先专注 25 分钟什么都行"。番茄 todo 顶部有大圆形按钮直接"开始 25 分钟专注"，专注记录到默认"未命名"或"快速专注"类。

**小克倾向**：TasksPage 顶部加一个小胶囊"🔥 快速开始 25 分钟"。点一下 → 直接建一个 task_keyword="快速专注"、default_minutes=25 的任务并 auto_start → 直达 FocusOverlay。

实现工作量：30 分钟。砸下去掉 4-5 步操作。

**优先级**：**中高**。不做会劝退"没想好做什么但就想专注的日子"。

---

### 💡 D-6. 没有通知 / 提醒

番茄 todo 有每日提醒（"晚上 9 点了，今天的番茄还没做完呢"）。Web app 能用 Web Push 做类似的，但需要后台 Service Worker + 服务端推送机制——**工作量大**。

**小克倾向**：**暂不做**。Web Push 链路太长，维护成本高；且用户通常把 app 打开着，声音+Web Notification（前台）已经覆盖 80% 场景。等 PWA 完善度上去再说。

**优先级**：**低**。不是迁移拦路石。

---

## 第二层：差异化反杀（番茄 todo 永远做不到的事）

如果只补上第一层，用户会留下但没惊艳。真正"再也回不去"的是：**用小白 / AI 陪伴的调性重新做番茄 todo 的核心交互**。

这部分不是代码 bug，是产品设计机会。D 视角提出来，但**不写具体代码方案**——需要 Sephy 做产品决定。

### 💎 反杀 1：数据页不做柱状图，做"小白的叙事"

番茄 todo 的数据页是工具型：柱状图 + 百分比 + 热力图。**这种视觉 AI 陪伴产品学过去会显得矛盾**。

**我们可以做的**：同样的数据，用小白的第一人称叙事重新包装。

举例（只是示意，最终文案要 Sephy 定）：
```
这周
小白陪你专注了 12 次
总共 285 分钟
你最稳的时段是 10:00-11:00
最常做的是"写论文"（4 次）

小白觉得
你这周比上周更稳定一点，连续专注的次数变多了
周三那天有点难，你建了 3 个任务但没做完——没关系，人有时就是这样
```

这个文案完全可以让 DS 基于 stats 数据实时生成（或用模板 + 变量）。番茄 todo 永远做不到这种。

### 💎 反杀 2：番茄结束不是"叮"一声而是小白探头

番茄 todo 的结束提示是功能性的（声音 + 通知弹窗）。**我们可以做仪式感**。

场景：`00:00` → 白鼬从遮罩右下角角落蹦出来 → 嘴里叼着一根松针（或戴着小耳机）→ 气泡说"到啦～休息 5 分钟，喝点水"。

**这种小动画就是你把 Clawd 研究搞明白的原因**——不是一模一样，但结构一样（pixel art 角色 + 状态机）。

### 💎 反杀 3：连续打卡不是火焰而是"松针林"

番茄 todo 用"🔥 连续 N 天"做成就感。

**我们可以用**：白鼬的松针林背景——连续打卡 7 天 = 林里多 3 棵松针，30 天 = 下雪了，100 天 = 林子成密林。**打卡数据和产品视觉直接绑定**，断了卡不是"失去数字"而是"林子枯了一片"，挽留感比数字强。

（这和 project_progress.md 里写的"松针雪地"主视觉呼应——让这个 motif 承担更多叙事功能。）

### 💎 反杀 4：白噪音用小白的声音 / 场景

番茄 todo 的白噪音是通用音源（雨 / 咖啡馆 / 火）。

**我们可以在通用音源基础上叠加小白的细节**：
- "松林"白噪音 = 风声 + 偶尔松针掉落声 + 很偶尔小白吸鼻子的声音
- "炉火"白噪音 = 柴火声 + 很久一次小白翻身声

这种音源是可以自己合成的（几段公共素材 + 小白音效叠加）。做出来独一份。但也可以先上通用版本再慢慢加，不要一开始就卡在这。

### 💎 反杀 5：AI 主动关怀（番茄 todo 死活做不到）

番茄 todo 是被动工具——用户不开它就什么也不说。

**我们可以**：小白在检测到以下场景时主动说话——
- 用户 3 天没开 app → 下次打开时气泡"我记挂你好几天了，你去哪了？"
- 用户连续 5 个番茄都完成 → "你今天很稳啊，要不要先休息下？"
- 用户昨天建了但没做 → "昨天的『写论文』你还想做吗？不想的话我帮你删掉"

这需要：session 打开时触发一次"主动问候"机制 + 基于 ai_memo / tasks 状态做条件判断。**这才是我们产品真正的护城河**，番茄 todo 没有这个能力。

---

## 三、D 视角优先级建议

要让"番茄 todo 用户试用 → 留下"路径通，**四件必做**（按烫度）：

1. **D-1 番茄自动循环**（3-4h，最大单点瓶颈）
2. **D-3 数据页做出来**（先做最简版，2-3h）—— 配 💎 反杀 1 的文案用 DS 生成
3. **D-2 白噪音**（1-2h）—— 配 💎 反杀 4 调性
4. **D-4 专注结束提示**（2-3h）—— 配 💎 反杀 2 小白探头

这四件合起来大约 **10-12h**，做完可以说"基础不退步"。

**然后是差异化反杀**（护城河，Sephy 决定要不要做）：

5. 💎 连续打卡 → 松针林视觉（配桌宠第 3 步做）
6. 💎 AI 主动关怀（打开时触发机制）

---

## 四、D 视角没提、为什么

- **任务拖拽排序 / 搜索 / 批量** —— 番茄 todo 有，但不是迁移拦路石。属于 C 视角的"加功能"。
- **任务和番茄的多对一关系**（一个任务做 N 个番茄）—— 是产品范式差异，要 Sephy 决定要不要改。**不是代码 bug**。
- **Web Push 通知**（D-6）—— 工作量大回报中，不值得现在做。
- **锁机 / 强制专注** —— Web app 做不了完整锁机，能做的也只是 visibilitychange 警示——已经在 C 视角顺手提过。

---

## 五、D 视角最核心的一句话

**不要直接抄番茄 todo**。产品真正的护城河是小白、是白鼬、是漫画气泡、是 AI 记忆。

如果花 50h 去抄一个番茄 todo 的柱状图、锁机、社区——我们会做出一个"有 AI 的差劲番茄 todo"。番茄 todo 核心功能背后有巨大的产品投入积累，追不上也没必要追。

**正确路径**：
- 第一层（基础能力）**补到 80 分就行**，够用、不退步即可
- 第二层（差异化反杀）**做到 120 分**，让番茄 todo 永远做不了

这样番茄 todo 用户试用几天后会这样想："这里的基础功能够我用，而且**我好像离不开那只白鼬了**"。

这才是"再也回不去"的机制。

---

## 六、留给后续小克的上下文

- 本文档 2026-04-22 写，是视角序列的第四份（最后一份）。
- D 视角和 A/B/C 的职责不重叠：A/B 是**现有功能怎么变好**，C 是**现有功能怎么变美**，D 是**还有什么功能该加才够**。
- **D-1 / D-2 / D-3 / D-4 四件合起来做**才是一个完整番茄闭环，**一件一件做只修不到 50% 的体验**。
- 第二层的"反杀"是产品方向级的，**不要自己决定怎么做**——要和 Sephy 对方案。
- A/B/C/D 合起来看，v2 从代码到产品还差的东西相当多。**不要被清单吓到**——关键是选哪条先做。
