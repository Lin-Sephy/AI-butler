# 桌宠设计：冬毛白鼬"小白"

> 2026-04-14 立项，2026-04-16 调整分工。
> 素材在 `docs/references/白鼬/`，已落地 SVG 在 `frontend/src/assets/`。

---

## 分工（重要，2026-04-16 改）

- **Sephy 出 SVG**：11 张状态图全部由 Sephy 在画图工具里画，多色 trace 导出 SVG
- **小克接图 + 写状态机**：拿到 SVG 后 copy 到 `frontend/src/assets/`、砍蓝背景、按 state 切 src、写优先级/打断逻辑

**小克不要自己画 SVG**——Sephy 的画工 + 风格定调 > 小克手写 path。下面的"形象规格 / 配色"是 **Sephy 自检画稿用的对照清单**，不是给小克的 design spec。

---

## 形象规格（Sephy 自检对照清单）

### 风格路线：偏玩偶化（不是写实）

**比例：**
- 头身比 **1 : 2.5**（介于玩偶 1:2 与写实 1:3.5 之间）
- 眼睛占头部 **1/4 - 1/5**（比玩偶克制，比真实醒目）
- 身体**修长但曲线柔和**，不追求解剖学肌肉，也不做圆球

**识别锚点（不能丢）：**
- 黑圆眼 + 粉红鼻尖 + 内耳粉（**脸部三锚**）
- 胡须 **4-5 根细长**（细节感是"不幼稚"的关键）
- **蓬松黑尾尖**（不是点，是一撮）
- 粉色四足小垫

**守住的底线：**
- ❌ 不走玲娜贝儿式巨头
- ❌ 不走写实冷感纤瘦
- ❌ 不做二次元吉祥物
- ✅ 参考对标：Apple Journal / Headspace 吉祥物的比例与调性

### 配色

| 部位 | 颜色 |
|---|---|
| 身体主色 | 纯白（#FFFFFF）带浅暖奶油暗部 |
| 眼 | 黑（#1A1A1A） |
| 鼻 + 内耳 + 肉垫 | 粉红（#F5B5B5 附近） |
| 尾尖 | 黑蓬松（#1A1A1A） |
| 胡须 | 浅灰白（半透明） |

---

## 状态机（v2 极简：6 state / 2 张新 SVG）

> 2026-04-17 从 11 必做砍到 6 state。砍法见本节末尾"v2 已砍"清单。
> "已落地 SVG"列：✅ 已画 + 接进代码 / 🟡 已画未接 / ❌ 待 Sephy 画

### 组 A：常态（3 state / 2 张 SVG + CSS）

| # | state | 素材 | 触发 | 动画 |
|---|---|---|---|---|
| 1 | idle | ✅ stoat-front | 默认（聊天室） | 呼吸起伏 + 偶尔尾摆（3-4s 循环） |
| 2 | listening | ✅ stoat-front（与 idle 同素材） | 用户输入框有字 | 可选 CSS 耳朵轻抖，无新 SVG |
| 3 | sleeping | ❌ 待 Sephy 画（**眼睛分层可 CSS 切闭/睁**） | 20 分钟无互动 / 专注遮罩默认 / 聊天室长挂 | 缓慢呼吸 4s 循环；眼睛默认闭，用户回到页面时切睁 1-2s 再闭 |

### 组 B：交互瞬间（1 state / 1 张 SVG）

| # | state | 素材 | 触发 | 动画 |
|---|---|---|---|---|
| 4 | thinking | ✅ stoat-side + 头顶泡泡 3 点 | AI API 调用中 | 侧脸 + 泡泡依次亮，持续到 API 返回 |

### 组 C：专注遮罩（2 state / 1 张复用 + 1 张新 SVG）

| # | state | 素材 | 触发 | 动画 |
|---|---|---|---|---|
| 5 | focus-glance | 🟡 stoat-side-half | 见下方"触发规则" | 抬头 1.5-2s → 回 sleeping |
| 6 | happy | ❌ 待 Sephy 画（= 跳起来） | 专注完成 / 聊天室任务记录成功 | 原地跳 + 尾翘，一次性 0.6s |

**微动作（叠加层，不占 state 优先级）：**
- **blink**：纯 CSS，idle / listening 上每 3-6 秒随机眨一次，0.2s one-shot

**#5 focus-glance 触发规则：**
- 专注遮罩内每 5-8 分钟随机一次（区间随机）
- 专注剩 1 分钟时强制一次（暗示快结束）
- 完成前 5 秒期待抬头
- 用户离开 Tab 超过 30 秒回来时抬一下
- `document.hidden === true` 时暂停所有计时和动画
- one-shot 完成后回落到 sleeping（专注遮罩里的持续态）

### v2 已砍（原 11 状态中的 7 个）

| 原状态 | 砍的理由 |
|---|---|
| intro 探头出现 | 打开直接 idle 即可，入场戏不必要 |
| 歪头在听（独立 SVG） | 现有 front 复用 OK；未来需要差异化再加 CSS 耳朵轻抖 |
| 好奇探身 curious | 信息增量低，砍 |
| 专注陪伴 focus-idle | sleeping 覆盖 |
| 跑动 running | 位置切换彩蛋，v2 不做 |
| 警觉 alert | 预定任务到期提醒 UI 还未实装，无触发场景 |
| 醒来 waking | sleeping 眼睛 CSS 切闭→睁代替整套起身序列 |

已画但 v2 不接的素材：🟡 stoat-cute（留作未来 listening 视觉升级的备用资产）。

---

## 优先级 & 打断逻辑

**优先级（高抢占低，one-shot 完成后回持续态）：**

```
8  thinking          — 可被用户打断
7  happy (one-shot)
5  listening
3  focus-glance (专注遮罩 one-shot，回落到 sleeping)
2  idle
1  sleeping (长时无操作 / 专注遮罩默认)
```

**思考打断规则：**
- 用户在 API 未返回时发新消息 / 切页 / 刷新
- 思考泡泡 fade-out 0.15s，小白立即切回 idle
- API 正常返回时：泡泡淡出 + 按上下文切下一个状态（idle / listening / happy）

**场景切换：**
- 聊天室 ↔ 专注遮罩 直接 opacity fade（0→1，0.2s ease-out），不走 intro 过渡
- 任务页 / 数据页 / 我的页 桌宠不出现

---

## 聊天页布局（2026-04-16 定，漫画气泡版）

> 此前 04-15 设计的"双 mode (Mode A 闲玩 / Mode B 聊天)"layout 已**被推翻**——实测时 mode 切换有割裂感，白鼬被压成 widget。改用**单一漫画式气泡布局**，白鼬永远主角。完整推翻过程见 commit `939c142` (双 mode savepoint) → `b76017e` (漫画版改造)。

### 当前布局（实现见 `frontend/src/pages/ChatPage.jsx`）

```
┌──────────────────────────────┐
│           📃  ⊕  ⚙️           │ ← 极简顶栏
├──────────────────────────────┤
│      ╭─────────────╮           │
│      │ 小白说...    │           │ ← 顶气泡：AI 最新一句，泡尾下指白鼬
│      ╰─────╲───────╯           │
│            ╲                   │
│        [白鼬全身居中 220px]      │ ← 永远主角，状态机活着
│            ╱                   │
│      [记下「xxx」][再聊聊]       │ ← 任务推荐按钮（如有），紧贴 AI 气泡下方
│                          ╭───╮ │
│                          │嗯 │ │ ← 你的气泡：右对齐到输入框右边，
│                          ╰───╯ │   堆叠 4 条，opacity 渐弱
├──────────────────────────────┤
│  [输入框________]    [发送]      │
└──────────────────────────────┘
```

### 关键约定

- **白鼬永远 full body 居中**，不分 mode
- **顶气泡（小白）** 只显最新一条，带泡尾向下指白鼬
- **底气泡（你）** 最多 4 条堆叠，最新在最下 opacity 1，老的渐弱（0.65 / 0.35 / 0.15），新消息推老的往上
- **任务推荐按钮** "[记下「xxx」N min] [再聊聊]" 紧贴 AI 气泡下方独立一行
- **气泡 >100 字**截断 + "更多"展开
- **thinking 时**：藏 AI 气泡 + 任务按钮，让白鼬头顶 3 点泡泡当唯一焦点；新回复来 sending 转 false → 显新气泡
- **历史视图**：右上 📃 按钮 → 切到传统消息列表（用 ChatBubble 组件）→ 左上 `<<` 返回主场
- **新建对话**：⊕ 按钮 confirm 后清空 messages（resetSession）

### 白鼬状态映射（StoatHalf.jsx，当前 Step 2a 实装）

| state | SVG | 何时触发 |
|---|---|---|
| `idle` | stoat-front 正脸 | 默认 |
| `listening` | stoat-front 正脸（同 idle） | 用户输入框有字 |
| `thinking` | stoat-side 侧脸 + 头顶 3 点泡泡 | API 调用中 |

Step 3 补齐 sleeping / focus-glance / happy 三个 state（sleeping / happy 等 Sephy 出新图，focus-glance 用已备的 stoat-side-half）。资产现状：`frontend/src/assets/stoat-{front, side, cute, side-half}.svg`；cute 暂不接，留作未来 listening 视觉升级备用。

---

## SVG 实现参考（Clawd 路线）

**要抄的结构模式**（在 `C:/Users/Administrator/Desktop/clawd-on-desk/src/state.js`，纯结构参考，不碰视觉资产）：

- **`STATE_PRIORITY` 优先级表** —— 高优先级抢占低优先级
- **`ONESHOT_STATES` 自动回落** —— 一次性状态完成后自动回持续态
- **`MIN_DISPLAY_MS` 最小展示时间** —— 防止状态瞬闪
- **`pendingTimer` 抢占排队** —— 高优先级 one-shot 插队时的排队机制
- **SVG 状态切换 = SVG 替换 + opacity fade 过渡**（0→1，0.2s ease-out）—— 掩盖资产边界，切换不突兀
- **每个状态一个 SVG 文件**，内部用 CSS `@keyframes` 做动画
- **身体部件保留 JS hook**（`#eyes-js`, `#body-js`）用于动态 transform

**要砍的（Electron 桌面端特有，Web 用不到）：**
- mini mode / 边缘吸附
- `BrowserWindow` / `setIgnoreMouseEvents` / 置顶窗口
- `ipcMain` ↔ preload bridge
- 进程检测 / focus terminal / 光标跨屏追踪

**可选（v2 先不做，桌宠打磨阶段再加）：**
- 光标追踪 —— 实现"专注中桌宠抬头看用户"（眼睛角度跟光标联动）

---

## 工作量估计（v2 极简版）

**Sephy 出图：** 2 张新 SVG 约 1-2h（sleeping + happy）
**小克接图 + 状态机：**
- 接图（copy / 砍背景 / state 切 src）：每张 5-10 min
- 状态机 + 优先级 + 触发逻辑 + blink CSS + sleeping 眼睛切换 CSS：3-4h
- 手机端实测 + 调：1-2h
- **合计前端约 4-6h**

总计 5-8h（对比原 11 状态方案 10-16h，省一半）。

---

## 素材现状（2026-04-17）

**已落地接入代码：**
- ✅ stoat-front 思考正脸 → idle / listening
- ✅ stoat-side 思考侧脸 → thinking + 头顶 3 点泡泡

**已画未接（留作 Step 3 统一接）：**
- 🟡 stoat-side-half 思考侧脸半身 → focus-glance（Step 3 接）
- 🟡 stoat-cute 歪头卖萌 → v2 不接，留作未来 listening 升级备用

**待 Sephy 画：**
- ❌ **sleeping 蜷睡**（眼睛分层，让小克用 CSS 切闭/睁；覆盖聊天长挂 + 专注遮罩默认 + 跨天"起身"过渡）
- ❌ **happy 开心蹦跳**（= 跳起来，覆盖专注完成 + 任务记录成功）

---

*改版前先看 `plan.md` 和此文件。Sephy 出新图后用一句话告诉小克"X 状态画好了"，小克接 + 改本表 ✅。*
