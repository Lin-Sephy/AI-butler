# 后端死代码清理清单

> 已发现、但本次不清、等指定时机统一收的死代码 / 遗产条目登记处。
>
> **维护原则**：发现 → 登记 → 等时机。不在日常改动里顺便清，因为跨系统清理容易踩坑、也分散 review 注意力。清的时候一次收齐更干净。

---

## ~~Streamlit MVP 退役时统一清~~（2026-04-22 已完成）

整段 app.py 在 v5 上线前已删除（webapp-migration 分支）。以下条目（#1 / #2 / #4 / #8）全部随 app.py 一并清理：
`start_idle_task/start_scheduled_task` 旧引用、`just_sharing + find_matching_task` 死分支及 `find_matching_task` 函数、`get_follow_up` / `FOLLOW_UP_MESSAGES` 追问池、app.py 的 persona + v4 信号块 / 推任务遗留。

<details>
<summary>历史归档（展开看完整旧条目）</summary>

### 1. app.py 引用已合并的旧函数 `start_idle_task` / `start_scheduled_task`

- 位置：`app.py:20, 22, 164, 180`
- 背景：后端小克在 2026-04-17 把 `start_idle_task` / `start_scheduled_task` 合并为 `start_task(allowed_from=...)`，api.py 已同步，app.py 未同步
- 清理动作：
  - 删 import（line 20, 22）
  - 把 `_on_start_idle` / `_on_start_scheduled` 里的调用替换为 `start_task(..., allowed_from=("idle",))` / `start_task(..., allowed_from=("scheduled",))`
- 来源：`docs/conversations/与小克对话/2026-04-17-后端测试.txt` L739-853

### 2. app.py:680-686 `just_sharing + find_matching_task` 死分支

- 位置：`app.py:680-686`
- 代码片段：
  ```python
  mentioned = signal.get("mentioned_activity")
  if mentioned and signal.get("user_attitude") == "just_sharing":
      match = find_matching_task(mentioned, today_tasks)
      if match and match["status"] in ("executing", "paused"):
          # 用户可能在说完成了某个进行中的任务，但不自动标记，
          # 让用户手动点完成更安全
          pass
  ```
- 问题：最内层是 `pass`，整段分支实际什么都不做
- 清理动作：
  - 删整段 if 块
  - 同步检查 `core/rules_engine.py:find_matching_task` 的其他调用者——如果除此外无别处使用，函数一起删
- 已验证（2026-04-17 再次核查）：`api.py:31` 的 `find_matching_task` import 是**死 import**（api.py 整文件无调用点）；`tests/test_rules_engine.py` 有 5 个 case 在测它。v2 里 `find_matching_task` 无实际调用者，可随 app.py 一起删（函数 + api.py import + 5 个 test case）
- 发现时间：2026-04-17

### 4. `get_follow_up` / `FOLLOW_UP_MESSAGES` 在 v2 里无接入

- 位置：`core/rules_engine.py:159-169`（函数体 + 常量池）
- 唯一调用：`app.py:14` 的 import（Streamlit MVP 里给"再聊聊"按钮做随机追问消息用）
- 问题：v2 api.py 不 import、不调用；v2 前端的"再聊聊"按钮交互形态改了，用不到追问池
- 清理动作：MVP 退役时连同 `app.py` 的 import + `FOLLOW_UP_MESSAGES` 常量 + `get_follow_up()` 函数一起删
- 发现时间：2026-04-17

### 8. app.py 的 persona 引用 & Streamlit MVP 所有 v4 信号块 / 推任务逻辑

- 背景：v5.0 重构（2026-04-20）把 `call_chat` / `call_task` / `get_chat_prompt` / `get_task_prompt` 的 `persona` 参数都砍了（MBTI 三套人设改为 custom_persona 的预设模板，不再是独立字段）
- v5.0 上线后 app.py 在 webapp-migration 分支上**会直接 TypeError**，现不管
- 具体位置：
  - `app.py:113-125` 的 `st.session_state.persona` + 侧边栏 radio
  - `app.py:596` 的 `call_chat(persona=st.session_state.persona, ...)`
  - `app.py:633` 的 `call_task(persona=st.session_state.persona, ...)`
- 同时 app.py 还有一堆 v4 架构遗留（信号块 6 字段、推任务规则引擎、精力偏差检测、call_task 整套流程）——这些在 v5.0 里全部作废，跟 Streamlit MVP 一起退役
- 清理动作：Streamlit Cloud 正式下线时，删整个 `app.py` 和 Streamlit 相关 requirements / 部署配置；保留 `streamlit-mvp` git tag 做历史归档
- 发现时间：2026-04-20

</details>

---

## 数据库 Schema 稳定后 migrate 删列

### 3. action_log 的 `intent` / `strategy` 字段

- 位置：`db/database.py` 的 `save_action_log()` 签名 `intent: str = "", strategy: str = ""`
- 背景：MVP 阶段规则引擎三分支设计遗留。新规则引擎（v4+）已不使用
- 当前处置：后端小克保留默认空串，新调用处不传值（见 `2026-04-17-后端测试.txt` L340-349, L771-782）
- 清理动作：
  - DB migrate 删 `intent` / `strategy` 列
  - 函数签名去掉这两个参数
  - 评估旧数据是否归档

---

## v2 内部清理（随时可清，不阻塞 MVP）

**建议清理节奏**：每做完一大步（第 2 / 3 / 4 / 5 步）后各做一次 sweep，把累积的"随时可清"条目一次收齐。如果某步拖超过一周，单独留 30min slot 做一轮也行。**避免在功能开发 commit 里顺手夹清理**——跨系统清理容易踩坑、也分散 review 注意力，单独 commit 更干净。

### 5. `should_trigger_task` 的死参数 `energy_level` / `task_board`

- 位置：`core/rules_engine.py:14`，签名 `should_trigger_task(signal, energy_level, task_board)`
- 问题：函数体只读 `signal`，`energy_level` 和 `task_board` 两个参数从头到尾未使用。`api.py:188` 调用处传了真实值，`tests/test_rules_engine.py` 的 7 个 case 也都传了假值（`4` / `[]`）。签名不诚实
- 评估：
  - `energy_level` **真死**——精力兜底由 `validate_reply` 的 ENERGY_RULES 处理，触发判断不需要看精力
  - `task_board` **可能是留给未来的钩子**——未来规则引擎可能要看任务栏（避免在有执行中任务时再推新任务），保留到真要实装时再说
- 清理动作（可随时执行）：
  - 砍 `energy_level` 形参 + `api.py:188` 的传入 + `tests/test_rules_engine.py` 7 个 case 的 `4`
  - `task_board` 保留（维持未来钩子）或一起砍（看实装意向）
- 发现时间：2026-04-17

### 6. `core/memory.py` `_get_impressions` 的旧格式兼容分支

- 位置：`core/memory.py` 的 `_get_impressions()`
- 代码片段：
  ```python
  try:
      data = json.loads(raw)
      if isinstance(data, dict) and "impressions" in data:
          return data["impressions"]
      return []
  except json.JSONDecodeError:
      return []
  ```
- 问题：`except json.JSONDecodeError → []` 和 `不满足 dict 且含 impressions 键 → []` 两个 fallback 分支是为了兼容 Streamlit MVP 时代 ai_memo 存纯文本的旧数据
- 评估：v2 用全新 Supabase 项目 `ieakiihfsaqqyyjdyjtt`，不可能存在 pre-v2 数据；`_save_impressions` 是唯一写入点，总是写合法 JSON。两个分支**从 v2 视角看全是死的**
- 清理动作：
  - 简化成 `return json.loads(raw).get("impressions", []) if raw else []`（更激进）
  - 或保留 try/except 作为防御性（不信任外部数据写入），只删注释里的"兼容旧格式"说法
- 发现时间：2026-04-16（首次 simplify 时标为"保守留着无碍"，补录）

### 7. `config.py` `SUPABASE_ANON_KEY` 未被 backend 消费

- 位置：`config.py:16`，`SUPABASE_ANON_KEY = os.getenv("SUPABASE_ANON_KEY", "")`
- 问题：backend 代码（`core/` / `db/` / `api.py`）一处都没 `config.SUPABASE_ANON_KEY` 引用；`scripts/smoke.py` 虽然用到 anon key，但**直接读 `os.getenv`，不走 config.py**
- 评估：真死。anon key 是前端的活（见 `frontend/.env.example` / `frontend/src/lib/supabase.js`），backend 只用 `SUPABASE_SERVICE_KEY`
- 清理动作：删 `config.py:16` 这一行；`scripts/smoke.py:36` 继续走 `os.getenv` 不受影响
- 发现时间：2026-04-17（补录——grep 时钓出来的）

### ~~8~~. `call_task` 及 v4 任务推荐遗产——✅ 2026-04-22 已清

- 删除：`core/intent.py` 的 `call_task` / `_parse_task_response` / `_validate_task_fields` / `EMPTY_SIGNAL`；`prompts/system_prompt.py` 的 `TASK_PROMPT_TEMPLATE` / `get_task_prompt` / `build_task_message`
- 副作用：`call_chat` 返回 dict 里的 `signal` key 一并移除（不再有调用方读它）

### ~~9~~. `/api/plan/extract` + `plan_extractor.py` 整个模块——✅ 2026-04-22 已清

- 删除：`api.py` 的 `plan_extract` endpoint + `PlanExtractRequest` + `extract_tasks_from_conversation` import；`core/plan_extractor.py` 整个文件；前端 `chatApi.js` 的 `planExtract()`

### 10. `call_chat` 的死参数 `energy_level`

- 位置：`core/intent.py` `call_chat` 签名 + `build_chat_message` 签名
- 问题：v5 prompt 不再用精力档位，但 `energy_level` 形参还在；api.py 把 `energy_level` 固定成 3 传进来，内部只在 `FALLBACK_TEMPLATES.get(energy_level, ...)` 兜底回复时用到——但 FALLBACK_TEMPLATES 的按档分档在 v5 也不是重点
- 清理动作：
  - 砍 `call_chat` 和 `build_chat_message` 的 `energy_level` 形参
  - 砍 `api.py` 里 `energy_level = req.energy_level or 3` 这行 + `call_chat(req.message, energy_level, ...)` 的第二个位置参数
  - `FALLBACK_TEMPLATES` 简化成单条固定兜底文案（v5 不需要按档分档）
- 注：`session_summary` / `is_cross_day` 已在 2026-04-22 Commit 1（Streamlit 退役）清掉
- 发现时间：2026-04-22（本条部分已做，剩 energy_level）

### ~~11~~. `ChatResponse` v4 字段 + `recordTask` export——✅ 2026-04-22 已清

- 删除：`api.py` `ChatResponse` 的 `signal` / `task_recommendation` / `show_action_buttons` / `energy_confirm_needed` 4 个字段 + return 对应赋值；前端 `chatApi.js` 的 `recordTask()`

---

## v5 代码 review 发现的待修项（2026-04-22，非死代码，但登记避免忘）

> 这一批不是死代码而是**功能/一致性 bug**。放这里是因为 review 一次性发现多条，独立修单 TODO 容易丢。
> 按紧急度分档处理，P0/P1 建议尽快动，P2 可以攒一起跟 v2 内部清理一起收。
> 修完的条目直接删。

### ~~A1~~. `update_ai_memory` 孤儿——✅ 2026-04-22 已修

- 修法：`/api/chat` 结尾用 FastAPI `BackgroundTasks` 异步跑 `update_ai_memory`，每 20 轮用户消息触发一次（对齐 call_chat 的 20 条窗口）

### ~~A2~~. `_tool_query_schedule` event 死分支——✅ 2026-04-22 已修

- 修法：砍 `task_type == "event"` 过滤，改用 `scheduled_at.startswith(today)`，任意今日 scheduled 的任务都算事件

### ~~A3~~ `create_tasks` 工具 `energy_level=3` 硬编码——✅ 2026-04-22 已修

改传 `None`，DB `energy_at_start` 写 NULL

### ~~A4~~ `_tool_query_stats` 完成率算错——✅ 2026-04-22 已修

分母改成 `completed + abandoned`（真尝试过的），不把 idle/scheduled 未开始的算成"没完成"

### ~~A5~~ `minutes` 范围校验不一致——✅ 随 #9 自动消失

`plan_extractor.py` 已在 commit 2 整模块删掉，只剩 `plan_tools.py:386` 的 `max(1, min(480, ...))`

### ~~A6~~ `PlanConfirmModal` 改 task_type 静默不落库——✅ 2026-04-22 已修

UI 把 work/rest chip 换成只读 span，title 提示"类型在任务页改"。后端想加 endpoint 解锁再说

### ~~A7~~ 前端 `mode` 不持久化——✅ 2026-04-22 已修

`ChatContext` 加 localStorage 读写（key `ai-butler-chat-mode`），setMode 包一层统一落盘

### A8. CORS `allow_origins=["*"]`（上线前必改）

- 位置：`api.py:48`
- 问题：开发期标注全放行，v5 已上 Render。正式上线前必须收紧到 Vercel 前端域名
- 修法：环境变量 `ALLOWED_ORIGINS` 配白名单
- 发现时间：2026-04-22（已登记，上线前处理）

### A9. Minor 一些（发现时间：2026-04-22，留痕）

- `_parse_chat_reply` 闲聊模式也解析 judgment 块——当前不会误触发弹窗（前端 `created.length > 0` 守门），但 semantically 冗余。prompt 层可加强"闲聊模式严禁输出 judgment 块"
- `MODE_SWITCH_HINT` 是弱指令，可能被 DS 漏或过度触发——实测观察
- `SettingsPanel.jsx:120` `Promise.all` 一错全错，某个字段失败时其他已成功的改动用户看不出来——改 `Promise.allSettled`
- `ChatPage` 的 `recentUsers` 用 `messages.length` 计算 key，消息变动时 React 会 remount 触发动画——换用稳定 key
- `call_chat` 的 `MAX_TOOL_ROUNDS = 15` 无"是否进展"检测，如果 DS 陷循环调同一工具会烧 15 次调用费——日志能看到就行

---

## 本次信号字段精简（2026-04-17 同步处理，此处仅留痕）

以下不进入"等时机"清单，本次随信号字段精简一并清掉：

- 信号块 `emotion` 字段
  - `core/intent.py:38` defaults
  - `prompts/system_prompt.py:95, 100` prompt 里的字段和描述
  - **与 `memory.py` 的 `emotion` 无关**——记忆系统走独立的 `extract_and_update` LLM 调用，从对话文本自己抽取
- 信号块 `scheduled_time` 字段
  - `core/intent.py:42` defaults
  - `prompts/system_prompt.py:95, 104` prompt 里的字段和描述
  - **与 call_task 的 `scheduled_at` 无关**——预定任务功能走 call_task JSON 的 scheduled_at 路径（`app.py:651-662` + `core/intent.py:209-220`）

---

*本文件随发现增补，每次统一清理后把对应条目删除即可。*
