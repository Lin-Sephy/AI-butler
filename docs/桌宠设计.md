# 桌宠设计：冬毛白鼬"小白"

> 2026-04-14 立项，2026-04-16 调整分工。
> 素材在 `docs/references/白鼬/`，已落地 SVG 在 `frontend/src/assets/`。

---

## 分工（重要，2026-04-16 改）

- **Sephy 出 SVG**：11 张状态图全部由 Sephy 在画图工具里画，多色 trace 导出 SVG
- **小克接图 + 写状态机**：拿到 SVG 后 copy 到 `frontend/src/assets/`、砍蓝背景、按 state 切 src、写优先级/打断逻辑

**小克不要自己画 SVG**——Sephy 的画工 + 风格定调 > 小克手写 path。下面的"形象规格 / 配色"是 **Sephy 自检画稿用的对照清单**，不是给小克的 design spec。

---

## 形象规格（Sephy 自检对照清单）

### 风格路线：偏玩偶化（不是写实）

**比例：**
- 头身比 **1 : 2.5**（介于玩偶 1:2 与写实 1:3.5 之间）
- 眼睛占头部 **1/4 - 1/5**（比玩偶克制，比真实醒目）
- 身体**修长但曲线柔和**，不追求解剖学肌肉，也不做圆球

**识别锚点（不能丢）：**
- 黑圆眼 + 粉红鼻尖 + 内耳粉（**脸部三锚**）
- 胡须 **4-5 根细长**（细节感是"不幼稚"的关键）
- **蓬松黑尾尖**（不是点，是一撮）
- 粉色四足小垫

**守住的底线：**
- ❌ 不走玲娜贝儿式巨头
- ❌ 不走写实冷感纤瘦
- ❌ 不做二次元吉祥物
- ✅ 参考对标：Apple Journal / Headspace 吉祥物的比例与调性

### 配色

| 部位 | 颜色 |
|---|---|
| 身体主色 | 纯白（#FFFFFF）带浅暖奶油暗部 |
| 眼 | 黑（#1A1A1A） |
| 鼻 + 内耳 + 肉垫 | 粉红（#F5B5B5 附近） |
| 尾尖 | 黑蓬松（#1A1A1A） |
| 胡须 | 浅灰白（半透明） |

---

## 状态机（11 必做 + 1 可选）

> "已落地 SVG"列：✅ 已画 + 接进代码 / 🟡 已画但没接 / ❌ 待 Sephy 画
> 接进代码后小克更新此列；Sephy 出新图后用一句话告诉小克"X 状态画好了"，小克接图 + 标 ✅

### 组 A：出现 / 待机 / 微动作

| # | 状态 | 已落地 SVG | 触发 | 动画 | 时长 |
|---|---|---|---|---|---|
| 1 | 探头出现 intro | ❌ | App 启动 / 切到聊天页 / 苏醒 | 从雪地探头 + 头微摆 | 一次性 0.8s |
| 2 | 基础待机 idle | ✅ stoat-front | 默认 | 呼吸起伏 + 偶尔尾摆 | 3-4s 循环 |
| 3 | 眨眼 blink | ❌（叠加 idle） | 每 3-6 秒随机 | 眼闭 0.1s 再睁 | 0.2s one-shot |
| 4 | 蜷睡 sleeping | ❌ | 20 分钟无互动 | 缓慢呼吸 | 4s 循环 |

### 组 B：聊天互动

| # | 状态 | 已落地 SVG | 触发 | 动画 | 时长 |
|---|---|---|---|---|---|
| 5 | 歪头在听 listening | 🟡 stoat-cute（已画未接，目前复用 stoat-front） | 用户正在打字（输入框有字） | 头歪 + 耳朵微抖 | 2s 循环 |
| 6 | 思考中 thinking | ✅ stoat-side + 头顶泡泡 3 点 | AI API 调用中 | 侧脸 + 泡泡 3 点依次亮 | 持续到 API 返回 |
| 7 | 开心蹦跳 happy | ❌ | 用户赞美 / 任务完成 / 记录成功 | 原地跳 + 尾翘 | 一次性 0.6s |
| 8 | 好奇探身 curious | ❌ | 用户提新话题 / 触发新印象 | 前倾 + 耳立 | 一次性 1s |

### 组 C：专注遮罩

| # | 状态 | 已落地 SVG | 触发 | 动画 | 时长 |
|---|---|---|---|---|---|
| 9 | 专注陪伴 focus-idle | ❌ | 专注开始后默认 | 坐着 + 极弱呼吸 | 4-5s 循环 |
| 10 | 偶尔抬头 focus-glance | 🟡 stoat-side-half（已画未接） | 见下方"触发规则" | 抬头 1.5-2s → 回 idle | 2s one-shot |

**#10 触发规则：**
- 每 5-8 分钟随机一次（区间随机）
- 专注剩 1 分钟时强制一次（暗示快结束）
- 完成前 5 秒期待抬头
- 用户离开 Tab 超过 30 秒回来时抬一下
- `document.hidden === true` 时暂停所有计时和动画

### 组 D：过渡 / 特殊

| # | 状态 | 已落地 SVG | 触发 | 动画 | 时长 |
|---|---|---|---|---|---|
| 12 | 跑动 running | ❌ | 位置切换 / 彩蛋 | 侧身跑动循环 | 0.4s 循环 |
| 13 | *警觉 alert*（可选） | ❌ | 预定任务到期提醒 | 突然站立 + 头转向 | 一次性 0.8s → idle |
| 14 | 醒来 waking | ❌ | sleeping → idle 过渡 | 完整起身序列 | 一次性 2-2.5s |

---

## 优先级 & 打断逻辑

**优先级（高抢占低，one-shot 完成后回持续态）：**

```
8  思考中 (thinking)      — 可被用户打断
7  开心蹦跳 / 好奇探身 / 警觉 (one-shot)
5  歪头在听 (listening)
3  专注陪伴 / 偶尔抬头 (专注遮罩)
2  眨眼 / 基础待机
1  蜷睡 (sleeping，长时无操作默认)
```

**思考打断规则：**
- 用户在 API 未返回时发新消息 / 切页 / 刷新
- 思考泡泡 fade-out 0.15s，小白立即切回 idle
- API 正常返回时：泡泡淡出 + 按回复内容切下一个状态（listening / happy / curious）

**场景切换：**
- 聊天室 ↔ 专注遮罩 之间通过 `intro` 状态过渡
- 任务页 / 数据页 桌宠不出现

---

## 聊天页布局（2026-04-16 定，漫画气泡版）

> 此前 04-15 设计的"双 mode (Mode A 闲玩 / Mode B 聊天)"layout 已**被推翻**——实测时 mode 切换有割裂感，白鼬被压成 widget。改用**单一漫画式气泡布局**，白鼬永远主角。完整推翻过程见 commit `939c142` (双 mode savepoint) → `b76017e` (漫画版改造)。

### 当前布局（实现见 `frontend/src/pages/ChatPage.jsx`）

```
┌──────────────────────────────┐
│           📃  ⊕  ⚙️           │ ← 极简顶栏
├──────────────────────────────┤
│      ╭─────────────╮           │
│      │ 小白说...    │           │ ← 顶气泡：AI 最新一句，泡尾下指白鼬
│      ╰─────╲───────╯           │
│            ╲                   │
│        [白鼬全身居中 220px]      │ ← 永远主角，状态机活着
│            ╱                   │
│      [记下「xxx」][再聊聊]       │ ← 任务推荐按钮（如有），紧贴 AI 气泡下方
│                          ╭───╮ │
│                          │嗯 │ │ ← 你的气泡：右对齐到输入框右边，
│                          ╰───╯ │   堆叠 4 条，opacity 渐弱
├──────────────────────────────┤
│  [输入框________]    [发送]      │
└──────────────────────────────┘
```

### 关键约定

- **白鼬永远 full body 居中**，不分 mode
- **顶气泡（小白）** 只显最新一条，带泡尾向下指白鼬
- **底气泡（你）** 最多 4 条堆叠，最新在最下 opacity 1，老的渐弱（0.65 / 0.35 / 0.15），新消息推老的往上
- **任务推荐按钮** "[记下「xxx」N min] [再聊聊]" 紧贴 AI 气泡下方独立一行
- **气泡 >100 字**截断 + "更多"展开
- **thinking 时**：藏 AI 气泡 + 任务按钮，让白鼬头顶 3 点泡泡当唯一焦点；新回复来 sending 转 false → 显新气泡
- **历史视图**：右上 📃 按钮 → 切到传统消息列表（用 ChatBubble 组件）→ 左上 `<<` 返回主场
- **新建对话**：⊕ 按钮 confirm 后清空 messages（resetSession）

### 白鼬状态映射（StoatHalf.jsx）

| state | SVG | 何时触发 |
|---|---|---|
| `idle` | stoat-front 正脸 | 默认 |
| `listening` | stoat-front 正脸（同 idle） | 用户输入框有字 |
| `thinking` | stoat-side 侧脸 + 头顶 3 点泡泡 | API 调用中 |

11 状态完整状态机（含 happy / curious / sleeping / waking 等）留 Step 3 做。资产已备：`frontend/src/assets/stoat-{front, side, cute, side-half}.svg`。

---

## SVG 实现参考（Clawd 路线）

**要抄的结构模式**（在 `C:/Users/Administrator/Desktop/clawd-on-desk/src/state.js`，纯结构参考，不碰视觉资产）：

- **`STATE_PRIORITY` 优先级表** —— 高优先级抢占低优先级
- **`ONESHOT_STATES` 自动回落** —— 一次性状态完成后自动回持续态
- **`MIN_DISPLAY_MS` 最小展示时间** —— 防止状态瞬闪
- **`pendingTimer` 抢占排队** —— 高优先级 one-shot 插队时的排队机制
- **SVG 状态切换 = SVG 替换 + opacity fade 过渡**（0→1，0.2s ease-out）—— 掩盖资产边界，切换不突兀
- **每个状态一个 SVG 文件**，内部用 CSS `@keyframes` 做动画
- **身体部件保留 JS hook**（`#eyes-js`, `#body-js`）用于动态 transform

**要砍的（Electron 桌面端特有，Web 用不到）：**
- mini mode / 边缘吸附
- `BrowserWindow` / `setIgnoreMouseEvents` / 置顶窗口
- `ipcMain` ↔ preload bridge
- 进程检测 / focus terminal / 光标跨屏追踪

**可选（v2 先不做，桌宠打磨阶段再加）：**
- 光标追踪 —— 实现"专注中桌宠抬头看用户"（眼睛角度跟光标联动）

---

## 工作量估计

**Sephy 出图：** 每张 SVG 0.5-1h（多色 trace + 调形），11 状态约 6-10h
**小克接图 + 状态机：**
- 接图（copy / 砍背景 / state 切 src）：每张 5-10 min
- 状态机 + 优先级 + 触发逻辑（一次写完）：3-4h
- **合计前端约 4-6h**

---

## 素材现状（2026-04-16）

**已落地（front + side 接进 StoatHalf）：**
- ✅ stoat-front 思考正脸 → idle / listening
- ✅ stoat-side 思考侧脸 → thinking + 头顶 3 点泡泡

**已画但没接（assets 里有，等状态机完整时接）：**
- 🟡 stoat-cute 歪头卖萌 → 候选 listening 状态
- 🟡 stoat-side-half 思考侧脸半身 → 候选专注遮罩 focus-glance

**待 Sephy 画（按优先级）：**
- ❌ 蜷睡 sleeping（坐 / 趴 / 仰睡 任选一）
- ❌ 醒来 waking（一次性序列，可静态 + fade）
- ❌ 探头出现 intro
- ❌ 开心蹦跳 happy（任务记录成功/赞美 反馈用）
- ❌ 好奇探身 curious
- ❌ 专注陪伴 focus-idle（坐姿 vs 躺姿任选）
- ❌ 跑动 running（彩蛋，可延后）
- ❌ 警觉 alert（可选）

---

*改版前先看 `plan.md` 和此文件。Sephy 出新图后用一句话告诉小克"X 状态画好了"，小克接 + 改本表 ✅。*
